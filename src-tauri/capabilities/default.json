{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for local Codex aggregate commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
